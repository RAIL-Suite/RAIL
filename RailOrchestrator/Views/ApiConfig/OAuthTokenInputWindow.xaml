<Window x:Class="WpfRagApp.Views.ApiConfig.OAuthTokenInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OAuth Token Input" 
        Width="400" Height="350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1E1E1E">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="OAuth Token Input" FontSize="18" FontWeight="SemiBold" 
                   Foreground="White" Margin="0,0,0,8"/>
        <TextBlock Grid.Row="1" Text="Paste the tokens from your OAuth callback:" 
                   Foreground="#888" FontSize="12" Margin="0,0,0,16"/>
        
        <StackPanel Grid.Row="2">
            <TextBlock Text="Access Token" Foreground="White" FontSize="12" Margin="0,0,0,4"/>
            <TextBox x:Name="AccessTokenInput" Background="#2D2D2D" Foreground="White" 
                     BorderBrush="#444" Padding="8,6" Margin="0,0,0,12"/>
            
            <TextBlock Text="Refresh Token (optional)" Foreground="White" FontSize="12" Margin="0,0,0,4"/>
            <TextBox x:Name="RefreshTokenInput" Background="#2D2D2D" Foreground="White" 
                     BorderBrush="#444" Padding="8,6"/>
        </StackPanel>
        
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Padding="16,8" Background="#333" Foreground="White" 
                    BorderThickness="0" Cursor="Hand" Click="CancelButton_Click"/>
            <Button Content="Save" Padding="16,8" Background="#007ACC" Foreground="White" 
                    BorderThickness="0" Cursor="Hand" Margin="8,0,0,0" Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>

